{
  "name": "auth-email-server",
  "version": "1.0.0",
  "description": "Simple server to send confirmation codes via SMTP (for local testing)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.3"
  }
}
